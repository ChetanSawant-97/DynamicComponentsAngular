import { PublishTimePipe } from './publish-time.pipe';

describe('PublishTimePipe', () => {
  it('create an instance', () => {
    const pipe = new PublishTimePipe();
    expect(pipe).toBeTruthy();
  });
});
